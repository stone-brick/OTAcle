<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.otacle.remote.repository.ProjectMapper">
    <resultMap id="BaseResultMap" type="com.otacle.remote.model.Project">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="project_path" property="projectPath"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into projects(name, description, project_path, created_at, updated_at)
        values(#{name}, #{description}, #{projectPath}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        select * from projects where id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select * from projects
    </select>

    <update id="update">
        update projects set name = #{name}, description = #{description}, project_path = #{projectPath}, updated_at = #{updatedAt} where id = #{id}
    </update>

    <delete id="deleteById">
        delete from projects where id = #{id}
    </delete>

    <select id="selectByName" resultMap="BaseResultMap">
        select * from projects where name = #{name}
    </select>
</mapper>